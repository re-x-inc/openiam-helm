apiVersion: v1
kind: Service
metadata:
  name: {{ include "openiam.fullname" . }}
  labels:
    {{- include "openiam.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openiam.selectorLabels" . | nindent 4 }}
